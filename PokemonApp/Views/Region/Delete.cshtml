@model Application.ViewModels.RegionViewModel
@{
    ViewData["Title"] = "Delete";
}

<div class="row">
    <div  class="card"> 
        <div class="card-header bg-danger text-light">

            <h4>Estas seguro de querer eliminar la REGION @Model.Name</h4>
        </div>
        <div class="card-bpdy">
            <form method="post" asp-controller="Region" asp-action="DeletePost">
                <input type="hidden" asp-for="Id" />
                <button type="submit" class="btn btn-danger">Eliminar </button>
                <a asp-controller="Region" asp-action="Index" class="btn btn-warning">Cancelar </a>
            </form>
        </div>
    </div>

</div>

